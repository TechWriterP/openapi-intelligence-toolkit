# Production test data

This directory contains curated production fixtures only. See `docs/testing.md`
for layout, provenance, determinism, and golden-update requirements.

The empty capability directories establish ownership without introducing feature
fixtures. Add evidence only with the production behavior that consumes it.
