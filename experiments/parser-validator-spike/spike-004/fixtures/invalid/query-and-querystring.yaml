openapi: 3.2.0
info: { title: Query combination, version: 1.0.0 }
paths:
  /invalid:
    query:
      operationId: invalidCombination
      parameters:
        - { name: q, in: query, schema: { type: string } }
        - { name: whole, in: querystring, content: { application/x-www-form-urlencoded: { schema: { type: object } } } }
      responses: { '200': { description: ok } }
