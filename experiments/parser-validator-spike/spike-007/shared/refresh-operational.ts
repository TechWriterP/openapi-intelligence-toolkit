import{createHash}from'node:crypto';import{readFile,writeFile}from'node:fs/promises';import{join}from'node:path';import{root}from'./benchmark-runner.js';const operational=JSON.parse(await readFile(join(root,'results/operational-evidence.json'),'utf8'));for(const name of['scalar','redocly','ibm']){const path=join(root,'results',`${name}.json`),result=JSON.parse(await readFile(path,'utf8'));result.environment=operational.environment;result.dependencyInventory=operational;delete result.internalResultHash;result.internalResultHash=createHash('sha256').update(JSON.stringify(result)).digest('hex');await writeFile(path,JSON.stringify(result,null,2)+'\n')}console.log('refreshed shared operational evidence and internal hashes')
